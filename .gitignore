# Build artifacts
build/*.o
build/*.obj
build/*.res
build/certs/

# Private key material - NEVER commit
certs/*.key
certs/*.pfx
certs/*.p12
certs/cert.cnf

# Compiled binaries (distributed via GitHub Releases, not committed)
dist/macos/RigiconLive
dist/macos/RigiconLive-universal
dist/macos/RigiconLive-arm64
dist/macos/RigiconLive-x86_64
dist/linux/RigiconLive
dist/windows/RigiconLive.exe
dist/windows/*.pdb

# Stray file that some `install` command versions create in cwd
/install

# Editor / OS
.DS_Store
.vscode/
.idea/
*.swp
